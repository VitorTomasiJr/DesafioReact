import React from 'react';

const ButtonVoltar = ({ onClick }) => {
    return (
        <button className="btn btn-secondary mb-3" onClick={onClick}>Voltar</button>
    )
};

export default ButtonVoltar