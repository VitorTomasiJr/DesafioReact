import React from 'react';

const ButtonDetalhar = ({ onClick }) => {
    return (
        <button className="btn btn-primary" onClick={onClick}>Detalhar</button>
    )
};

export default ButtonDetalhar